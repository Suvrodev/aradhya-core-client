import { useTitle } from "../../../Component/hook/useTitle";

const AdminProfile = () => {
  useTitle("Admin Profile");
  return (
    <div>
      <h1>Admin Profile</h1>
    </div>
  );
};

export default AdminProfile;
